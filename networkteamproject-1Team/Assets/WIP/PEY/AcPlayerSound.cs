using UnityEngine;

// AnimationEvent를 받기 위해 존재 (없으면 에러라 빨리 만들어둠)
public class AcPlayerSound : MonoBehaviour
{
    private void OnFootstep(AnimationEvent animationEvent)
    {
    }

    private void OnLand(AnimationEvent animationEvent)
    {
    }
}
